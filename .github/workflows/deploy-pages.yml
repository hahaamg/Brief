name: Deploy Slide1 files to GitHub Pages

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare Pages artifact from Slide1 files
        run: |
          if [ ! -d Slide1 ]; then
            echo "Slide1 directory not found."
            exit 1
          fi

          shopt -s nullglob
          files=(Slide1/*)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No files found in Slide1 directory."
            exit 1
          fi

          mkdir -p dist/Slide1
          cp -r "${files[@]}" dist/Slide1/

          if [ ! -f dist/index.html ]; then
            html_files=(dist/Slide1/*.html)
            if [ ${#html_files[@]} -gt 0 ]; then
              cp "${html_files[0]}" dist/index.html
            fi
          fi

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
